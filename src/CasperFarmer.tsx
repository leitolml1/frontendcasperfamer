import React from 'react'
import { RouterProvider } from 'react-router-dom'
import { appRouter } from './app.router'

export const CasperFarmer = () => {
  return (
    <RouterProvider router={appRouter}>

    </RouterProvider>
  )
}
